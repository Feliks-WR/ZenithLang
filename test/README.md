# Test directory for Zenith

This directory contains tests for the Zenith language and compiler.

## Running Tests

```bash
cd build
ninja check-zenith
```

## Test Structure

- `Dialect/` - Tests for dialect operations
- `Parser/` - Tests for parser functionality
- `Transforms/` - Tests for transformation passes

