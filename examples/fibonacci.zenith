// Fibonacci sequence generator
fibonacci(n) {
    a = 0
    b = 1
    i = 0
    
    while i < n {
        print a
        temp = a + b
        a = b
        b = temp
        i = i + 1
    }
    
    return a
}

main() {
    print "Fibonacci sequence (first 10):"
    result = fibonacci(10)
    return 0
}
